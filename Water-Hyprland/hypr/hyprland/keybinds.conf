# Mouse
bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

# Activity
bind=SUPER,Q,killactive

# Volume controller
binde = ,XF86AudioLowerVolume, exec, pactl -- set-sink-volume 0 -5%
binde = ,XF86AudioRaiseVolume, exec, pactl -- set-sink-volume 0 +5%
bind = ,XF86AudioMute, exec, pactl -- set-sink-mute 0 toggle
bind = ,XF86AudioMicMute, exec, pactl -- set-source-mute 0 toggle
binde = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-
binde = ,XF86MonBrightnessUp, exec, brightnessctl s +5%

# Fuzzel apps
bind=SUPER+ALT,N,exec,bash -c ~/.config/water-hyprland-global-scripts/exec-scripts/open-fuzzel-swaync-options.sh
bind=SUPER+ALT,S,exec,bash -c ~/.config/water-hyprland-global-scripts/exec-scripts/open-fuzzel-screen-capture.sh
bind=SUPER+ALT,U,exec,bash -c ~/.config/water-hyprland-global-scripts/exec-scripts/open-fuzzel-utility.sh
bind=SUPER+ALT,P,exec,bash -c ~/.config/water-hyprland-global-scripts/exec-scripts/open-fuzzel-powermenu.sh
bind=SUPER,Print,exec,bash -c ~/.config/water-hyprland-global-scripts/exec-scripts/open-fuzzel-screen-capture.sh

# Apps
bind=SUPER,E,exec,alacritty -e yazi
bind=SUPER,RETURN,exec,alacritty
bind=SUPER+ALT,SLASH,exec,fuzzel
bind=SUPER,N,exec,swaync-client -t
bind=SUPER+SHIFT,E,exec,wlogout

# Playerctl
bind=SUPER, equal, exec,playerctl next
bind=SUPER, minus, exec,playerctl previous
bind=SUPER+CTRL,P,exec,playerctl play-pause

# Clipboard
bind = Super, V, exec, pkill fuzzel || cliphist list | fuzzel  --match-mode fzf --dmenu | cliphist decode | wl-copy # Clipboard history >> clipboard
bind = Super+Shift, V, exec, pkill fuzzel || cliphist list | fuzzel  --match-mode fzf --dmenu | cliphist delete # Delete item in clipboard
bind = Super+Alt, V, exec, cliphist wipe && notify-send "Cleared clipboard history!"# Clear clipboard history

# Workspace
bind=SUPER+ALT,down,movefocus,d
bind=SUPER+ALT,up,movefocus,u
bind=SUPER+ALT,left,movefocus,l
bind=SUPER+ALT,right,movefocus,r

binde=SUPER,left,resizeactive,-40 0
binde=SUPER,right,resizeactive,40 0

binde=SUPER,up,resizeactive,0 -40
binde=SUPER,down,resizeactive,0 40

bind=SUPER+CTRL,left,movewindow,l
bind=SUPER+CTRL,right,movewindow,r
bind=SUPER+CTRL,up,movewindow,u
bind=SUPER+CTRL,down,movewindow,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind=SUPERSHIFT,1,movetoworkspacesilent,1
bind=SUPERSHIFT,2,movetoworkspacesilent,2
bind=SUPERSHIFT,3,movetoworkspacesilent,3
bind=SUPERSHIFT,4,movetoworkspacesilent,4
bind=SUPERSHIFT,5,movetoworkspacesilent,5
bind=SUPERSHIFT,6,movetoworkspacesilent,6
bind=SUPERSHIFT,7,movetoworkspacesilent,7
bind=SUPERSHIFT,8,movetoworkspacesilent,8
bind=SUPERSHIFT,9,movetoworkspacesilent,9
bind=SUPERSHIFT,0,movetoworkspacesilent,10
